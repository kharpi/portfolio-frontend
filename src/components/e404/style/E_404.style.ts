import styled from 'styled-components';
import tw from 'twin.macro';

export const SE404Container = styled.section`
	${tw`
    grid
    place-items-center
    h-screen
  `}
`;

export const SE404Text = styled.h2`
	${tw`
  font-medium 
  text-xl
`}
`;
